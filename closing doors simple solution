
code:
#include <stdio.h>

int main()
{
	int t;
	scanf("%d",&t);
	long long int n,i,j,count=0;
	while(t--)
	{
	    count=0;
	    scanf("%lld",&n);
	    for(i=1;i*i<=n;i++)
	    {
	        count++;
	    }
	    printf("%lld\n",count);

	}
	return 0;
}
